[tool.poetry]
name = "client-hub-auth-lib"
version = "0.1.0"
description = "Client Hub Auth Library"
packages = [{include = "middlewares", from = "src"}]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.11"
